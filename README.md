# Getting Started with Node and React App

### `git clone https://github.com/hmassonn/carto`
### `cd carto && cd server && npm i && npm start`
dans un autre shell
### `cd carto && cd client && npm i && npm start`
